# Kopieren nach .env im Projektverzeichnis:
#   cp deploy/env.example .env

SAQ_VENDOR_DIR=./vendor
SAQ_HOST=0.0.0.0
SAQ_PORT=8080
SAQ_MAX_UPLOAD_MB=100

# Optional: API-Schutz für öffentlichen Betrieb
# SAQ_API_KEY=dein-geheimer-schlüssel
